{
  "name": "mlx-ts-workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "clean": "npm run -ws clean"
  }
}
